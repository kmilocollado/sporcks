{%- comment -%}
  Bloque de navegación del carrusel (flechas). Solo en Lista de colecciones con diseño Carrusel.
  Añádelo donde quieras (arriba, junto al encabezado, abajo) y reordénalo con los demás bloques.
{%- endcomment -%}
{%- if section.settings.layout_type == 'carousel' -%}
  <div
    class="collection-list-carousel-nav-slot"
    data-carousel-nav-slot="collectionList"
    {{ block.shopify_attributes }}
  ></div>
{%- endif -%}

{% schema %}
{
  "name": "t:names.collection_list_carousel_nav",
  "tag": null,
  "settings": []
}
{% endschema %}
